import 'package:flutter/material.dart';

@immutable
class ExceptionsConstants {
  const ExceptionsConstants._();

  // Firebase
  static const String authError =
      "An unexpected problem occurred in the Auth system, the account was forcibly closed";
}
